export enum GroupName {
  Layout = 'layout',
  People = 'people'
}

export enum BasicsComponentType {
  Image = 'image',
  Btn = 'btn',
  Switch = 'switch',
  Input = 'input',
  Text = 'text',
  Textarea = 'textarea',
  Iconfont = 'iconfont',
  Tag = 'tag',
  Rate = 'rate',
  Link = 'link',
  Video = 'video'
}

export enum OtherComponentsType {
  Layout = 'layout'
}